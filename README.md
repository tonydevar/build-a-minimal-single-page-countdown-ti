# build-a-minimal-single-page-countdown-ti
Antigravity project: Build a minimal single-page countdown timer app with HTML, CSS, and JavaScript. Single self-contained file, no dependencies. The user can enter minutes and seconds, then count down to zero with a visual SVG progress ring. Clean, modern UI.
